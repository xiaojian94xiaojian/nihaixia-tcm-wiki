# derived

这里保存从 `raw/` 原始资料生成的派生文件，例如长文稿的章节切片。

## 使用规则

- `derived/` 不是事实来源，事实核对应回到 `raw/` 原文。
- 这里的文件可以删除后重建。
- 不要手工改写切片正文；如需修正内容，应先修正或替换 `raw/` 原始资料。
- AI 处理长资料时可优先读取 `chunks/`，减少上下文浪费。

## 当前子目录

- `chunks/`：按资料名分组的语义切片。
