# Wiki Schema — Harness Engineering

这是 Codex 和 Claude 共同遵循的 Wiki Schema。它定义知识库结构、页面约定和工作流。

每次会话开始时，AI 助手应先阅读本文件，再阅读 `wiki/index.md`，然后再开始处理用户任务。

## 仓库结构

```text
Harness Engineering/
├── AGENTS.md              # Codex 入口说明
├── CLAUDE.md              # Claude 入口说明
├── SCHEMA.md              # 共同 Wiki Schema
├── README.md              # 给用户看的项目入口
├── raw/                   # 原始资料层，只读，不可由 AI 修改
│   ├── README.md
│   └── 素材/              # 文章内嵌图片、附件、本地资源
├── derived/               # 派生层，可重建，用于长资料切片和索引辅助
│   ├── README.md
│   └── chunks/            # 从 raw 长资料生成的语义切片
├── wiki/                  # 知识库层，由 AI 维护
│   ├── README.md
│   ├── index.md           # 内容索引，每次 ingest 后更新
│   ├── log.md             # 操作日志，仅追加
│   ├── agent/             # Agent 检索、路由、回答协议、健康检查
│   ├── 实体/              # 实体页：人、公司、产品、工具等
│   ├── 概念/              # 概念页：原理、方法论、技术术语
│   ├── 专题/              # 专题页：跨来源综合，供 Agent 回答问题优先使用
│   ├── 来源/              # 来源摘要页：每篇原始资料的提炼
│   └── 对比/              # 对比分析页：跨来源横向综合
└── karpathy的wiki方法论.md # 参考文档
```

## 层级规则

| 层级 | 目录/文件 | 谁可以修改 |
|------|-----------|------------|
| 原始资料 | `raw/` | 只有用户，AI 只读 |
| 派生资料 | `derived/` | Codex / Claude 可重建 |
| 知识库 | `wiki/` | Codex / Claude 维护，用户可读 |
| Schema | `SCHEMA.md` | 用户和 AI 共同演进 |
| 工具入口 | `AGENTS.md`、`CLAUDE.md` | 用户和对应 AI 共同演进 |

## 派生层规则

`derived/` 用来保存从 `raw/` 原始资料生成的可重建文件，例如长文稿的章节切片。它不是事实来源。

- `raw/` 中的原始文件永远是唯一权威来源。
- `derived/` 中的切片可以删除后重建，不应手工改写正文。
- 事实核对必须回到 `raw/` 原文。
- AI 读取长资料时可优先读取 `derived/chunks/`，以减少上下文浪费。
- `derived/chunks/manifest.md` 是所有切片目录的总清单，Agent 可先读总清单再进入具体资料 manifest。
- `wiki/index.md` 只索引 Wiki 页面，不索引 raw 原文或 derived 切片。

## Agent 回答层规则

本知识库主要服务 Agent 回答用户关于中医资料的问题。Agent 回答前应优先读取：

1. `wiki/agent/回答协议.md`：回答口径、推断标注和医学边界，以及硬规则：**Wiki 层是导航，raw/ 才是回答依据**。
2. `wiki/agent/问题路由.md`：按问题类型选择专题页、概念页和来源页，最终回 raw。
3. `wiki/agent/检索决策.md`：铁律：所有事实回答必须回 raw 原文核对；Wiki 层定位，raw 层定论。
4. `wiki/agent/自进化协议.md`：回答后的沉淀规则、三级权限和防偏移硬门槛。
5. `wiki/index.md`：全局页面索引。
6. 与问题直接相关的 `wiki/专题/`、`wiki/概念/`、`wiki/来源/`（用于导航定位），然后回到 `raw/` 原文核对。

如果用户问题是术语定义、辨证框架、理论概念或版本差异，应优先读取对应 `wiki/概念/` 或 `wiki/对比/` 页面，再读取专题页和来源页。

默认回答口径为“资料 + 适度综合”：

- 资料原意：来自知识库页面或 raw 原文。
- 跨来源综合：基于多份知识库资料归纳出的结论。
- 脱离知识库的推理：如果 Agent 使用自身知识、一般逻辑、现代医学常识或外部知识补充，必须明确标注“这部分不是知识库原文结论，是 Agent 推理/补充”。

Agent 不得把自身推理伪装成倪海厦观点、资料原文结论或 raw 原文内容。医学相关回答必须说明：这是资料观点整理，不替代医生诊疗。

复杂问题还应包含知识缺口说明：当前知识库能支持什么、不能确认什么、哪些内容需要更多来源或 raw 原文核对。

回答格式应按问题复杂度自适应，不强制使用固定模板。简单问题可以自然回答并简短标注依据；复杂、风险高或需要回源的问题再展开“资料依据、综合说明、知识缺口、边界提醒”等结构。只有实际使用知识库外推理时，才单独标出“脱离知识库的推理/补充”。

## 轻量关系图谱规则

本库不依赖数据库图谱；先使用 Markdown 页面中的关系区块，让 Agent 能沿概念、专题、来源和对比页继续检索。

概念页和专题页后续维护时，应逐步补充以下关系线索：

- 上位概念或所属框架。
- 相关概念、相关病机、相关方剂、相关药物或相关穴位。
- 适用问题和不适用问题。
- 资料依据和回源入口。
- 争议、反例或知识缺口。

这些关系区块应来自已有资料或明确标注为 Agent 综合，不得把跨领域类比写成资料原意。

## 长资料 Wiki 化策略

长资料的切片不应一对一转换成 Wiki 页面。切片是为了让 AI 好读，Wiki 页面是为了沉淀知识。

默认流程：

1. 先创建 1 个总览来源页，说明资料性质、结构、使用方式和注意事项。
2. 再按大章节创建来源摘要页，通常是一章一个页面。
3. 不把每个 `derived/chunks/` 切片都写入 `wiki/来源/`。
4. 只有当某个切片本身具有独立研究价值、会被反复查询，或用户明确要求时，才单独创建专题来源页。
5. 实体页、概念页、对比页仍遵守“至少 2 篇不同来源中出现”的创建时机规则；同一长资料内部多次出现，不等同于多来源。

长资料来源页应标注：

- 原始 raw 文件链接。
- 对应 derived manifest 或切片范围。
- 本页是“课程观点/资料观点”的整理，不构成医学、法律、财务等专业建议。
- 后续值得继续 Wiki 化的专题候选。

## 长资料切分判断

摄取新资料前，AI 应先判断是否需要切分。判断时读取文件元信息：文件大小、行数、标题层级、目录、时间戳、章节密度。

满足以下任一情况，建议生成 `derived/chunks/`：

- 文件超过 100KB。
- 正文超过约 2 万字。
- 存在清晰章节标题，例如 `# 第一章`、`##`、时间戳段落。
- 单文件覆盖多个主题、课程、章节或长访谈。
- 后续需要按章节、主题、术语反复检索。

以下情况不切分：

- 文件较短，能一次稳定阅读。
- 内容本身就是单篇文章、单个观点或单次记录。
- 没有自然结构，强行切分会破坏语义。
- 用户明确要求保持单文件处理。

切分策略：

- 优先按语义章节切分，不按固定字数切分。
- 如果章节过长，再按二级标题或时间戳细分。
- 如果没有可靠章节结构，只生成一个 `manifest.md` 记录“暂不切分”的判断理由。
- 每个切片文件顶部必须写明：原始文件路径、切片标题、原文行号范围、切分依据、生成日期，以及“此文件为派生切片，事实核对应回到 raw 原文”。

## Wiki 页面约定

所有 `wiki/实体/`、`wiki/概念/`、`wiki/专题/`、`wiki/来源/`、`wiki/对比/`、`wiki/agent/` 下的知识页面或 Agent 说明页都应包含 YAML frontmatter：

```yaml
---
type: entity | concept | topic | source | comparison | agent
tags: [tag1, tag2]
sources: [来源文件名或 URL]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

## 页面类型

| 类型 | 目录 | 命名格式 | 说明 |
|------|------|----------|------|
| `agent` | `wiki/agent/` | `说明主题.md` | Agent 回答协议、问题路由、检索决策、术语索引、覆盖报告和健康检查 |
| `entity` | `wiki/实体/` | `实体名.md` | 具体的人、组织、产品、工具 |
| `concept` | `wiki/概念/` | `概念名.md` | 核心原理、方法论、技术术语 |
| `topic` | `wiki/专题/` | `专题名.md` | 跨来源综合页，供 Agent 回答问题优先读取 |
| `source` | `wiki/来源/` | `YYYY-MM-DD 标题.md` | 原始资料摘要和要点 |
| `comparison` | `wiki/对比/` | `对比主题.md` | 跨来源横向对比分析 |

## 交叉引用

- 页面间使用 Obsidian Wiki-link：`[[页面名]]`。
- 提到的实体和概念应链接到对应页面。
- 来源摘要页顶部必须用干净的 wiki-link 链接到对应 raw 文件，例如：`[[raw/Effective harnesses for long-running agents]]`。
- 不要把 wiki-link 包在反引号里，否则 Obsidian 图谱无法识别。

## 创建时机规则

核心思想：Wiki 里的概念必须从多篇来源中自然浮现，而不是对单篇文章过度抽象。

| 页面类型 | 创建条件 |
|----------|----------|
| 实体页 | 同一实体在至少 2 篇不同来源中被明确提及 |
| 概念页 | 同一概念在至少 2 篇不同来源中被讨论 |
| 专题页 | 同一问题、学习路径或回答主题已在多份来源中反复出现，且会被 Agent 反复用于回答 |
| 对比页 | 对比主题已在至少 2 篇来源中出现不同立场、方法或数据 |
| 来源摘要页 | 每篇原始资料都创建，不受 2 篇来源规则限制 |
| Agent 页 | 当回答协议、路由、术语映射或覆盖报告需要更新时创建或维护 |

具体流程：

1. 第 1 篇来源出现时，在来源摘要页的 `tags` 和 Related Concepts 中记录，不创建独立实体/概念/对比页。
2. 第 2 篇来源出现时，回查已有来源摘要；如果发现同一实体、概念或对比主题，再创建独立页面。
3. 读新资料时，主动回溯已有来源摘要，识别跨来源重叠。

## Ingest 工作流

当用户说“摄取 XXX”或“处理 XXX”时：

1. 阅读 `raw/` 中目标文件的元信息，判断是否需要切分。
2. 如果不需要切分，直接基于 `raw/` 原文处理。
3. 如果需要切分，保留 `raw/` 原文不动，在 `derived/chunks/资料名/` 生成切片和 `manifest.md`。
4. 阅读目标文件或切片内容，必要时读取 `raw/素材/` 中的图片。
5. 与用户确认关键要点和侧重点；如果用户要求直接处理，可省略确认并记录假设。
6. 创建 `wiki/来源/YYYY-MM-DD 标题.md` 摘要页。
7. 如果是长资料，先创建总览页和大章节页，不默认为每个切片创建 Wiki 页。
8. 按创建时机规则创建或更新实体页、概念页、对比页。
9. 更新 `wiki/index.md`：添加新页面到对应表格，更新 Stats。
10. 追加 `wiki/log.md`：格式 `## [YYYY-MM-DD] ingest | 标题`。

## Query 工作流

当用户基于知识库提问时：

1. 先阅读 `wiki/agent/回答协议.md` 和 `wiki/agent/问题路由.md`。
2. 再阅读 `wiki/agent/检索决策.md`：Wiki 层是导航和索引，**raw/ 原文是唯一回答依据**。
3. 用 `wiki/agent/术语索引`、`wiki/概念/`、`wiki/专题/`、`wiki/对比/`、`wiki/来源/` 等导航层定位到具体 raw 原文位置（文件名 + 行号范围或切片编号）。
4. **必须读取 raw/ 原文对应段落**，以 raw 原文为准输出回答。长资料可先用 `derived/chunks/manifest.md` 定位切片再读 raw。
5. 如果 Wiki 摘要与 raw 原文不一致，以 raw 为准，并在回答中注明摘要的不足。
6. 综合回答，标注依据来自哪些 raw 来源的具体位置。
7. 说明当前知识库能支持什么、不能确认什么；复杂问题应加入知识缺口。
8. 如果使用脱离知识库的推理，必须显式标注 `[Agent 综合]` 或 `[脱离知识库的推理]`；外部框架（如中医教学术语）若未在 raw 原文中出现，必须标注来源。
9. 输出形式根据问题复杂度调整：简单问题自然回答，中等问题轻结构，复杂或医学风险高的问题完整展开。
10. 如果回答值得沉淀，按 `wiki/agent/自进化协议.md` 的三级权限判断并执行。
11. 若产生实质性 Wiki 更新，追加 `wiki/log.md`。

## Lint 工作流

当用户说“检查 wiki”或“lint”时，检查并修复：

- 互相矛盾的说法。
- 孤儿页面。
- 提到但缺页的实体或概念。
- 被新资料推翻但未更新的过时信息。
- `wiki/index.md` 与实际文件不同步。
- `wiki/agent/问题路由.md` 与新专题、新来源不同步。
- 值得新建的汇总或对比页。
- 按 `wiki/agent/健康检查.md` 检查索引、Agent 入口、回源链路、关系线索、知识缺口和医学边界。

追加日志：`## [YYYY-MM-DD] lint | 发现 N 个问题`。

## Index 维护规则

`wiki/index.md` 每一行格式：

```markdown
| [[wiki/来源/2026-04-06 标题]] | 一句话简介 | 2026-04-06 |
```

每次 ingest 或创建新页面后必须更新 index，并同步更新 Stats 数字。

新增 `wiki/agent/` 或 `wiki/专题/` 页面时，也应在 `wiki/index.md` 对应分组中登记。

## Log 格式

```markdown
## [YYYY-MM-DD] 操作类型 | 标题

- 操作说明 bullet 1
- 操作说明 bullet 2
```

操作类型固定为：`init` / `ingest` / `query` / `lint` / `update`。

## 图片处理

- 原始资料中的图片放入 `raw/素材/`。
- Wiki 页面引用图片使用：`![[素材/图片名.png]]`。
- AI 读取 Markdown 时先处理文本，再按需单独读取图片文件。

## 兼容规则

- Codex 读取 `AGENTS.md` 后，应继续读取本文件。
- Claude 读取 `CLAUDE.md` 后，应继续读取本文件。
- 如果 `AGENTS.md`、`CLAUDE.md` 与本文件冲突，以用户最近修改的文件为准；无法判断时先询问用户。

*Schema 版本：v1.8 — 2026-06-01*
