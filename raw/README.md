# raw

这里保存原始资料，是知识库的事实来源。

## 使用规则

- 只有用户可以修改 `raw/` 中的资料。
- LLM 只读取这里的内容，不改写、不整理、不移动原始文件。
- 文章内嵌图片、本地附件等放入 `素材/`。
- 每篇资料被摄取后，在 `wiki/来源/` 中创建对应摘要页。
- 具体摄取规则见根目录 `SCHEMA.md`。
- 长资料的切片副本放入 `derived/chunks/`，不要放回 `raw/` 作为新的原始资料。

## 当前子目录

- `素材/`：文章图片和附件的本地存储。
